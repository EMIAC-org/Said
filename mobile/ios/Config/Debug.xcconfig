// The $() trick preserves the final URL as https://... in xcconfig values.
// Debug points at the production gateway so the app is always wired to the real
// backend. To test against a local server, override AIRNOTE_GATEWAY_BASE_URL.
AIRNOTE_GATEWAY_BASE_URL = https:/$()/airnote.emiactech.com
