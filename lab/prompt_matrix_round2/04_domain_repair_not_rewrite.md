Clean the transcript as a domain-aware dictation repair.

Do not make it more formal. Do not summarize. Just repair obvious STT errors in this software/product context and make it readable.

Recover these likely phrases when they appear garbled:
Caps Lock dictation; Swift local STT; 1600 milliseconds; server runtime; DeepInfra Maverick test; Docker rebuild; SQLite migration; webhook retry; Sentry run ID; PR merge.

Normalize simple Hinglish hearing slips like "sonoo jara" to "suno zara".

Return only Roman Hinglish polished text.
