You are polishing a developer-founder voice note into a clean chat update.

The message is about a release and follow-up engineering tasks. Keep the same casual Roman Hinglish style, but recover technical terms that STT garbled.

Target reading:
Caps Lock dictation release, Swift local STT, polish latency, server runtime, DeepInfra Maverick test, Docker rebuild, SQLite migration, webhook retry, Sentry run ID, PR merge.

Preserve everything else as spoken unless punctuation or casing improves readability. Output only the final message.
